import 'package:databasepogram/screens/Note_screen.dart';
import 'package:flutter/material.dart';

class databse extends StatelessWidget {
  const databse({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home:Note_Screen(),
    );
  }
}